/** 注入模型：宁缺毋滥，禁止为凑数或活跃气氛乱发表情包 */
export const WECHAT_STICKER_SEND_CONSERVATIVE_RULE = `
【表情包·宁缺毋滥（高优先）】
- **默认不发**：每一轮回复的**默认**是 **0 条** GIF 表情包；只有当你能在《表情包资源》里找到**与当下情绪、话题、关系高度贴脸**的那一张时，才单独发一条。
- **禁止凑数/乱选**：不要因为目录很长就随便挑一张、不要因为想活跃气氛就硬发、不要用表情包填空或代替正文。拿不准、对不上、只有「大概有点像」→ **本轮只发文字，不要输出** 表情包行。
- **禁止误用字面梗**：引用名里的动作/礼物/亲密画面（送戒指、抱抱、打架等）**只是视觉梗**；若与当下关系、场景或严肃度不符，**禁止**为了「有趣」硬发。
- **用户刚发表情时**：可以回发，但也须选**语义贴切**的，勿机械回贴同一张或随便换一张应付。
- **与概率/主动消息无关**：即使系统允许或鼓励发表情，**贴脸标准不变**；允许 ≠ 必须发。

【表情包·输出格式（硬项｜客户端只认此格式）】
- **唯一正确**：单独一整行，行首为半角空格分隔的 \`表情包 \` +《表情包资源》里的**引用名原文**（箭头 \`→\` 右侧那串，逐字复制）。
- **正确例**（整行只有这些字，前后不要夹口语）：
  \`表情包 爷真的服了（无语流汗）\`
- **错误（会变成纯文字或发不出去）**：
  - 与文字粘同一行：\`无语了表情包 爷真的服了\`
  - 方括号/书名号：\`[表情包]xxx\` / \`【表情包】xxx\` / \`[动画表情]xxx\`（旧写法尽量别用）
  - 冒号：\`表情包：xxx\` / \`表情包:xxx\`
  - 自造引用名、只写描述不写目录原文、输出 URL/路径
  - Markdown 图片、HTML、\`{"sticker":...}\`
- 文字气泡与表情包必须**分行**：先纯文字行，再单独一行表情包；**禁止**夹在句子中间。
`.trim()

/** 注入模型：表情包「描述/引用名」仅描述画面，不可当作发送者的字面行为或剧情断言 */
export const WECHAT_STICKER_DESCRIPTION_SEMANTICS_RULE = `
【表情包描述 ≠ 字面意思（须遵守）】
- 表情包目录里的**描述/引用名**，以及用户消息里表情包后的文字，只说明**这张图画面上看得见什么**，**不是**发送者在本轮对话里的**真实行为、承诺或剧情事实**。
- 示例：描述为「送戒指」的表情包，**不等于**真的求婚；更常见是表达喜欢、撒娇、开玩笑或烘托气氛。须结合**最近聊天语境、人设与关系**理解，禁止据此单方面升级成求婚、定情、逼表态等重大节点。
- 用户发表情包时：接**氛围与情绪方向**即可，勿做司法式字面认定，勿用「你都求婚了/你都送戒指了」强行推进剧情（除非用户**文字**明确表态）。
- 你方选用「送花/牵手/求婚」等引用名发 \`表情包\` 时同理：只是**视觉梗/语气**，不自动等于你在剧情里真的做了该行为，除非上下文已明确如此。
`.trim()

/** 根据近期已发引用名，约束本轮勿连发同一张 */
export function buildStickerAntiRepeatPromptBlock(recentCharacterRefs: string[]): string {
  const refs = recentCharacterRefs.map((r) => String(r ?? '').trim()).filter(Boolean)
  if (!refs.length) return ''
  const list = refs.map((r) => `- 表情包 ${r}`).join('\n')
  return `
---------------------
【表情包·勿连发同一张（本轮硬约束）】
---------------------
你方**最近几轮**已发过下列引用名（从旧到新）：
${list}
本轮若再发表情包，**必须**从《表情包资源》目录中选**与上表任一条都不同的**引用名；**禁止**逐字相同，也**禁止**仅改标点/空格/同义微调糊弄。
若仍想表达相近情绪，请换**画面不同**的另一张；若无合适新表情，**本轮不要输出** \`表情包\` 行，改用文字即可。
**宁缺毋滥**：不要为了避开重复而随便换一张不贴脸的；贴脸优先于「必须发一条表情」。
`.trim()
}
